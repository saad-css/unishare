Frontend setup
1) Install requirements: pip install -r requirements.txt
2) Update BASE_URL in unishare/core/config.py
3) Optional: place Cairo-Regular.ttf inside the frontend folder for better Arabic rendering
4) Run: python main.py
